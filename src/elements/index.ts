import BaseElement from './BaseElement';
import Rectangle from './Rectangle';

export enum ElementType {
  Arrow = 'Arrow',
  Rectangle = 'Rectangle',
  Triangle = 'Triangle',
  Circle = 'Circle',
}

export default {
  BaseElement,
  Rectangle,
};
